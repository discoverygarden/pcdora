---
name: Semantic Version Tagging
on:
  pull_request_target:
    types: closed
    branches:
      - 2.x

jobs:
  tag_update:
    name: Tag Update
    uses: discoverygarden/auto-semver/.github/workflows/semver.yml@v1
